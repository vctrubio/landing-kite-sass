import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'KiteFlow - Schedule your kite lessons effortlessly',
  description: 'All-in-one kite school management. Connect students with teachers, automate scheduling, and manage lessons with intelligent filtering and real-time updates.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}